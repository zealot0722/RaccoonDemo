{
  "name": "raccoon-ai-support-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "test": "node --test --test-isolation=none",
    "check": "node --check api/chat.js && node --check api/health.js && node --check api/tickets/index.js && node --check api/tickets/[id]/reply.js"
  },
  "engines": {
    "node": ">=20"
  }
}
